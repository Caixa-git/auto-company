#!/bin/bash
# .env 파일이 커밋되는 것을 방지하는 pre-commit hook
if git diff --cached --name-only | grep -q '^\.env$'; then
  echo "ERROR: .env 파일을 커밋할 수 없습니다!"
  echo "       config.yaml.example을 템플릿으로 사용하세요."
  exit 1
fi
